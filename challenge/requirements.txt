# India Runs Track-1 ranker — minimal, fully offline, CPU-only.
numpy>=1.26
scipy>=1.11
scikit-learn>=1.3
faker>=28.0        # synthetic data generation only
pandas>=2.0        # demo UI only
streamlit>=1.30    # demo UI only
pytest>=8.0        # tests only

# OPTIONAL semantic upgrade: with sentence-transformers installed AND the BGE
# weights vendored at challenge/models/bge-small/, the semantic stage uses BGE
# embeddings (384-dim) instead of TF-IDF — fully offline, loaded from disk.
# Toggle with the SEMANTIC_BACKEND env var: auto (default) | bge | tfidf.
# Not required: the system runs end-to-end on TF-IDF alone with no network.
# sentence-transformers>=3.0
# torch>=2.2            # CPU build recommended: --index-url https://download.pytorch.org/whl/cpu
#
# One-time vendoring (run once, online), then it works offline forever:
#   python -c "from sentence_transformers import SentenceTransformer as S; \
#              S('BAAI/bge-small-en-v1.5').save('challenge/models/bge-small')"
